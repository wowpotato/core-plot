    import XCTest
    @testable import core_plot

    final class core_plotTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(core_plot().text, "Hello, World!")
        }
    }
